/**
 * 
 * 节点列表
 * 
 *  const children = tree.root.children;
 *  
 *  children.add(node1);   // 添加节点到最后
 *  children.addBefore(node3, node1); // 添加节点到node1之前
*   children.addAfter(node3, node1); // 添加节点到node1之前
*   children.insert(node2,1)            // 插入节点到指定位置
*   
*   
* 
 */



export class FlexNodeList{
    
}


